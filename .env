# Vite environment — Spring Boot backend via Vite proxy
# Leave VITE_API_BASE_URL empty so axios uses relative /api/* paths
# Vite dev server proxies /api/* → http://localhost:8080/api/*
VITE_API_BASE_URL=

# Feature flags
VITE_ENABLE_REAL_TIME=true
VITE_DEBUG_MODE=false
